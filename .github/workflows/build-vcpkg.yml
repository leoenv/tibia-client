name: Build with vcpkg

on:
  workflow_dispatch:

  push:
    branches:
      - master
      - v*
    tags:
      - v*
    paths:
      - .github/workflows/**
      - cmake/**
      - src/**
      - CMakeLists.txt

  pull_request:
    paths:
      - cmake/**
      - src/**
      - CMakeLists.txt

jobs:
  job:
    name: ${{ matrix.os }}-${{ matrix.cxx }}-${{ matrix.buildtype }}
    runs-on: ${{ matrix.os }}-latest
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        name: [windows-msvc]
        buildtype: [Release]
        include:
          - name: windows-msvc
            os: windows
            cxx: cl.exe
            cc: cl.exe
            triplet: x64-windows
            packages: >
              boost-iostreams boost-asio boost-system boost-variant boost-lockfree boost-filesystem boost-uuid boost-thread
              glew luajit libogg libvorbis openal-soft opengl openssl physfs zlib

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Unshallow
        run: git fetch --prune --unshallow || true

      - name: Get latest CMake
        uses: lukka/get-cmake@latest

      - name: vcpkg install deps (runner pre-installed vcpkg, sem cache do GitHub)
        shell: pwsh
        run: |
          & "$env:VCPKG_INSTALLATION_ROOT\vcpkg.exe" install --triplet ${{ matrix.triplet }} ${{ matrix.packages }}

      - name: Setup MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: amd64

      - name: CMake configure & build (Ninja + MSVC)
        shell: pwsh
        env:
          # CMake 4 removeu suporte a cmake_minimum_required < 3.5 (OTClient pede 2.6/3.x antigo).
          # Passar via env var evita bug de parsing do "-DCMAKE_POLICY_VERSION_MINIMUM=3.5" na
          # linha de comando do Windows (o "3.5" era cortado em "3" + ".5", ver run #3).
          CMAKE_POLICY_VERSION_MINIMUM: "3.5"
        run: |
          cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT\scripts\buildsystems\vcpkg.cmake" -DVCPKG_TARGET_TRIPLET=${{ matrix.triplet }} -DLUAJIT=ON
          cmake --build build --parallel

      - name: dir
        run: Get-ChildItem -Recurse build | Select-Object FullName
        shell: pwsh

      - name: Upload artifact binary (windows exe + dlls)
        uses: actions/upload-artifact@v4
        with:
          name: otclient-windows-Release-${{ github.sha }}
          path: |
            build/otclient.exe
            build/*.dll
        if: contains( matrix.os, 'windows')
